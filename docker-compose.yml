services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: consulting
    ports:
      - "5432:5432"
    volumes:
      - consulting_pgdata:/var/lib/postgresql/data

volumes:
  consulting_pgdata:
